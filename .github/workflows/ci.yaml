name: C++ Makefile CI

on:
  push:
    branches: [main, development]
    tags: ['v*']
  pull_request:
    branches: [main, development]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Clang (Linux)
      if: runner.os == 'Linux'
      run: sudo apt-get update && sudo apt-get install -y clang

    - name: Setup MinGW (Windows)
      if: runner.os == 'Windows'
      uses: egor-tensin/setup-mingw@v2
      with:
        platform: x64
        verions: 13.0.0
      continue-on-error: true
    
    - name: Build
      run: make

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: mycli-${{ runner.os }}-${{ github.run_id }}
        path: dist/
        retention-days: 30

  check:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Clang
      run: sudo apt-get update && sudo apt-get install -y clang

    - name: Build
      run: make

    - name: Run Tests
      timeout-minutes: 1
      run: |
        printf "help\nexit\n" | ./dist/test_linux
        printf "greet\nexit\n" | ./dist/test_linux
    # adding static analytsic
    - name: Static Analytic
      run: |
        sudo apt-get install -y cppcheck
        cppcheck --enable=warning,style --error-exitcode=1 src/*.cc 2>/dev/null || true
  release:
    needs: [build, check]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
    - name: Download Artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          artifacts/mycli-Linux-*/*
          artifacts/mycli-Windows-*/*
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}